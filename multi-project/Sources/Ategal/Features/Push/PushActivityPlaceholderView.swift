import SwiftUI

struct PushActivityPlaceholderView: View {

    var body: some View {
        Color.white
            .ignoresSafeArea()
            .accessibilityIdentifier("push-activity-placeholder")
    }
}
